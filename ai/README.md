# ai

Projects in the AI/LLM domain.

| Project | Description |
| ------- | ----------- |
| [ollama](ollama/) | Local Open LLM runtime: a single server that runs models from the [ollama.com](https://ollama.com/library) registry (or user-created ones) and exposes them through a JSON REST API, an OpenAI/Anthropic-compatible API, and a CLI, with inference on CPU, CUDA, ROCm, and MLX plus tool calling, structured outputs, vision, embeddings, and cloud model offload. |
